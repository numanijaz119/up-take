# DEPRECATED. Files in this directory are retained for git history.
# The extension channel (src/channels/extension/) supersedes this.
# Nothing in this directory is imported by live code.
raise ImportError("src.channels.browser._archive is not for runtime import")
